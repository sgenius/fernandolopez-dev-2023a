import { Merriweather, Calistoga } from 'next/font/google'
export const merriweather = Merriweather({ weight: '400', subsets: ['latin'], fallback: ['sans-serif'] });
export const titleFont = Calistoga({ weight: '400', subsets: ['latin'], fallback: ['serif'] });